name: linting-python-code

on:
 push:
  branches:
   - master 
 pull_request:
  branches:
   - master 

jobs:
 python-linting-code:
 runs-on: ubuntu-latest
  steps:
   - name: Super-Linter
     uses: super-linter/super-linter@v5.7.2
            
   
